export const config = {
  width: 640,
  height: 480,
  facingMode: "user",
};

export const dtConfig = {
  inputResolution: {
    width: config.width,
    height: config.height,
  },
  scale: 0.8,
};
